import 'package:flutter/material.dart';

class TallerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Taller"),
      ),
      body: Center(
        child: Text("Pantalla Taller"),
      ),
    );
  }
}